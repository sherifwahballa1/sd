const Category = require('./category.model');
const CategoryVisitCount = require('./category.category-visit-count.model');
const categoryAPI = require('./category.API');

module.exports = {
  Category,
  CategoryVisitCount,
  categoryAPI
};