const Place = require('./place.model');
const Branch = require('./branch.model');
const Review = require('./review.model');
const placeAPI = require('./place.API');

module.exports = {
  Place,
  Branch,
  Review,
  placeAPI
};